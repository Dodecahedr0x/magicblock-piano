/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import { MAGICBLOCK_PIANO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PLAY_NOTES_DISCRIMINATOR = 1;

export function getPlayNotesDiscriminatorBytes() {
  return getU8Encoder().encode(PLAY_NOTES_DISCRIMINATOR);
}

export type PlayNotesInstruction<
  TProgram extends string = typeof MAGICBLOCK_PIANO_PROGRAM_ADDRESS,
  TAccountPiano extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPiano extends string
        ? WritableAccount<TAccountPiano>
        : TAccountPiano,
      ...TRemainingAccounts,
    ]
  >;

export type PlayNotesInstructionData = {
  discriminator: number;
  notes: Array<number>;
};

export type PlayNotesInstructionDataArgs = { notes: Array<number> };

export function getPlayNotesInstructionDataEncoder(): Encoder<PlayNotesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["notes", getArrayEncoder(getU8Encoder())],
    ]),
    (value) => ({ ...value, discriminator: PLAY_NOTES_DISCRIMINATOR }),
  );
}

export function getPlayNotesInstructionDataDecoder(): Decoder<PlayNotesInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["notes", getArrayDecoder(getU8Decoder())],
  ]);
}

export function getPlayNotesInstructionDataCodec(): Codec<
  PlayNotesInstructionDataArgs,
  PlayNotesInstructionData
> {
  return combineCodec(
    getPlayNotesInstructionDataEncoder(),
    getPlayNotesInstructionDataDecoder(),
  );
}

export type PlayNotesInput<TAccountPiano extends string = string> = {
  piano: Address<TAccountPiano>;
  notes: PlayNotesInstructionDataArgs["notes"];
};

export function getPlayNotesInstruction<
  TAccountPiano extends string,
  TProgramAddress extends Address = typeof MAGICBLOCK_PIANO_PROGRAM_ADDRESS,
>(
  input: PlayNotesInput<TAccountPiano>,
  config?: { programAddress?: TProgramAddress },
): PlayNotesInstruction<TProgramAddress, TAccountPiano> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MAGICBLOCK_PIANO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    piano: { value: input.piano ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.piano)],
    data: getPlayNotesInstructionDataEncoder().encode(
      args as PlayNotesInstructionDataArgs,
    ),
    programAddress,
  } as PlayNotesInstruction<TProgramAddress, TAccountPiano>);
}

export type ParsedPlayNotesInstruction<
  TProgram extends string = typeof MAGICBLOCK_PIANO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    piano: TAccountMetas[0];
  };
  data: PlayNotesInstructionData;
};

export function parsePlayNotesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPlayNotesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { piano: getNextAccount() },
    data: getPlayNotesInstructionDataDecoder().decode(instruction.data),
  };
}
