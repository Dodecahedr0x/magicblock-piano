/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
} from "@solana/kit";
import { findPianoPda, PianoSeeds } from "../pdas";

export type Piano = { bump: number; payer: Address; notes: Array<number> };

export type PianoArgs = Piano;

/** Gets the encoder for {@link PianoArgs} account data. */
export function getPianoEncoder(): Encoder<PianoArgs> {
  return getStructEncoder([
    ["bump", getU8Encoder()],
    ["payer", getAddressEncoder()],
    ["notes", getArrayEncoder(getU8Encoder())],
  ]);
}

/** Gets the decoder for {@link Piano} account data. */
export function getPianoDecoder(): Decoder<Piano> {
  return getStructDecoder([
    ["bump", getU8Decoder()],
    ["payer", getAddressDecoder()],
    ["notes", getArrayDecoder(getU8Decoder())],
  ]);
}

/** Gets the codec for {@link Piano} account data. */
export function getPianoCodec(): Codec<PianoArgs, Piano> {
  return combineCodec(getPianoEncoder(), getPianoDecoder());
}

export function decodePiano<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Piano, TAddress>;
export function decodePiano<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Piano, TAddress>;
export function decodePiano<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Piano, TAddress> | MaybeAccount<Piano, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPianoDecoder(),
  );
}

export async function fetchPiano<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Piano, TAddress>> {
  const maybeAccount = await fetchMaybePiano(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePiano<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Piano, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePiano(maybeAccount);
}

export async function fetchAllPiano(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Piano>[]> {
  const maybeAccounts = await fetchAllMaybePiano(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePiano(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Piano>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePiano(maybeAccount));
}

export async function fetchPianoFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PianoSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<Piano>> {
  const maybeAccount = await fetchMaybePianoFromSeeds(rpc, seeds, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePianoFromSeeds(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  seeds: PianoSeeds,
  config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<Piano>> {
  const { programAddress, ...fetchConfig } = config;
  const [address] = await findPianoPda(seeds, { programAddress });
  return await fetchMaybePiano(rpc, address, fetchConfig);
}
